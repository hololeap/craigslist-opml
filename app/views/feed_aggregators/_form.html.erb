<%= form_for(@feed_aggregator) do |f| %>
  <% if @feed_aggregator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@feed_aggregator.errors.count, "error") %> prohibited this feed_aggregator from being saved:</h2>

      <ul>
      <% @feed_aggregator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
